import 'package:flutter/material.dart';

class ConnectivityWidgetWrapper extends StatelessWidget {
  const ConnectivityWidgetWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
